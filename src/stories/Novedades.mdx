import { Meta } from '@storybook/addon-docs/blocks';
import novedades from './fixtures/novedades.json';
import { BotonSincronizar } from './BotonSincronizar';

<Meta title="Novedades" />

# Novedades

Este Storybook se puede sincronizar con el frontend real de Hermes (`Goberna-Lab/hermes`,
rama `main`) — hoy es manual (con el botón de abajo, o pidiéndolo en una sesión de
Claude Code). Cuando trae componentes nuevos o cambiados, queda un registro acá abajo —
así que basta con abrir esta página al entrar para saber qué cambió.

**"Sincronizar ahora" revisa `hermes/main` y trae lo nuevo a este repo** — corre
en tu máquina, no toca nada del monorepo real (solo lee de ahí), y deja un commit
local listo para pushear cuando quieras. Solo funciona con `npm run storybook`
corriendo local.

Cada sincronización lista los archivos que tocó, con la ruta completa y qué le
pasó a cada uno — <strong>A</strong> nuevo, <strong>M</strong> modificado,
<strong>D</strong> borrado, <strong>R</strong> renombrado — y a la derecha, dónde
vive ese componente en este Storybook. Los que dicen «sin story» son los que
todavía nadie cubrió: esa columna es la lista de trabajo pendiente.

<BotonSincronizar />

<div style={{ display: 'flex', flexDirection: 'column', gap: '1.5rem', marginTop: '1.5rem' }}>
  {novedades.map((n) => (
    <div
      key={n.fecha + n.commit}
      style={{
        border: '1px solid var(--sb-border, #E2E8F0)',
        borderRadius: '12px',
        padding: '1rem 1.25rem',
      }}
    >
      <div style={{ display: 'flex', alignItems: 'baseline', gap: '0.75rem', flexWrap: 'wrap' }}>
        <strong style={{ fontSize: '0.95rem' }}>{n.fecha}</strong>
        <code style={{ fontSize: '0.75rem', opacity: 0.65 }}>{n.commit}</code>
        {n.tipo === 'sync-automatico' && (
          <span
            style={{
              fontSize: '0.7rem',
              fontWeight: 700,
              textTransform: 'uppercase',
              letterSpacing: '0.02em',
              background: '#DBEAFE',
              color: '#1D4ED8',
              borderRadius: '999px',
              padding: '0.1rem 0.55rem',
            }}
          >
            auto
          </span>
        )}
        {n.tipo === 'sync-manual-boton' && (
          <span
            style={{
              fontSize: '0.7rem',
              fontWeight: 700,
              textTransform: 'uppercase',
              letterSpacing: '0.02em',
              background: '#EDE9FE',
              color: '#6D28D9',
              borderRadius: '999px',
              padding: '0.1rem 0.55rem',
            }}
          >
            botón
          </span>
        )}
      </div>
      <p style={{ margin: '0.5rem 0 0', lineHeight: 1.5 }}>{n.resumen}</p>
      {n.detalle && (
        <p style={{ margin: '0.5rem 0 0', lineHeight: 1.5, opacity: 0.8, fontSize: '0.9rem' }}>{n.detalle}</p>
      )}
      {/* Las entradas nuevas traen `cambios` (ruta + estado + dónde vive en
          Storybook). Las viejas solo tienen `archivos`, que era una lista de
          carpetas — se siguen dibujando como estaban. */}
      {n.cambios?.length > 0 ? (
        <table style={{ margin: '0.7rem 0 0', width: '100%', borderCollapse: 'collapse', fontSize: '0.82rem' }}>
          <tbody>
            {n.cambios.map((c) => (
              <tr key={c.archivo} style={{ borderTop: '1px solid var(--sb-border, #E2E8F0)' }}>
                <td style={{ padding: '0.35rem 0.5rem 0.35rem 0', width: '1%', whiteSpace: 'nowrap', verticalAlign: 'top' }}>
                  <span
                    title={
                      { A: 'Nuevo', M: 'Modificado', D: 'Borrado', R: 'Renombrado' }[c.estado]
                    }
                    style={{
                      display: 'inline-block',
                      minWidth: '1.15rem',
                      textAlign: 'center',
                      fontWeight: 700,
                      fontSize: '0.7rem',
                      borderRadius: '4px',
                      padding: '0.05rem 0.3rem',
                      background: { A: '#DCFCE7', M: '#DBEAFE', D: '#FEE2E2', R: '#FEF3C7' }[c.estado],
                      color: { A: '#166534', M: '#1D4ED8', D: '#991B1B', R: '#92400E' }[c.estado],
                    }}
                  >
                    {c.estado}
                  </span>
                </td>
                <td style={{ padding: '0.35rem 0.5rem 0.35rem 0', verticalAlign: 'top' }}>
                  <code style={{ fontSize: '0.78rem', wordBreak: 'break-all' }}>{c.archivo}</code>
                  {c.desde && (
                    <div style={{ opacity: 0.6, fontSize: '0.72rem' }}>
                      antes: <code style={{ fontSize: '0.72rem' }}>{c.desde}</code>
                    </div>
                  )}
                </td>
                <td style={{ padding: '0.35rem 0', textAlign: 'right', verticalAlign: 'top', whiteSpace: 'nowrap' }}>
                  {c.story ? (
                    <a
                      href={`/?path=/story/${c.storyId}`}
                      target="_top"
                      style={{ fontWeight: 600, fontSize: '0.78rem' }}
                    >
                      {c.story} →
                    </a>
                  ) : (
                    <span style={{ opacity: 0.45, fontSize: '0.75rem' }}>sin story</span>
                  )}
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      ) : (
        n.archivos?.length > 0 && (
          <ul style={{ margin: '0.6rem 0 0', paddingLeft: '1.1rem', fontSize: '0.85rem', opacity: 0.85 }}>
            {n.archivos.map((a) => (
              <li key={a}>
                <code>{a}</code>
              </li>
            ))}
          </ul>
        )
      )}
    </div>
  ))}
</div>

---

_Todo lo de acá arriba salió de una sincronización manual: "sync-manual" se hizo en una
sesión de Claude Code, "sync-manual-boton" con el botón de arriba. Una etiqueta "auto"
va a aparecer el día que se active una rutina programada — todavía no está armada (falta
un permiso de GitHub sobre `Goberna-Lab/hermes` a nivel de organización)._
