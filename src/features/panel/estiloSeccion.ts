/**
 * EL ENCABEZADO DE SECCIÓN DEL RESTYLE DE LA FICHA (20-ago-2026) — ícono +
 * texto más grande/oscuro que `sectionLabel` (`lib/styles.ts`, el token
 * compartido por TODO el repo, que no se toca). Este vive en `panel/` porque
 * el restyle nació acá, pero lo importan `FichaContacto`, `PersonaUnificada`
 * y `CarritoDeseado` — son presentación pura, sin acoplar datos ni lógica.
 */
export const encabezadoSeccion = 'mb-1.5 flex items-center gap-1.5 text-sm font-bold text-navy-ink';
